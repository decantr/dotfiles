#!/bin/sh

case "$1" in
	b*) PRE="env GDK_SCALE=2 " ;;
	s*) PRE="env GDK_SCALE=1 " ;;
	*) echo "invalid" && exit ;;
esac

shift

case "$1" in
	c) CMD=chromium ;;
	f) CMD=firefox ;;
	r) CMD=rider ;;
	*) CMD="$1" ;;
esac

shift

setsid $PRE "$CMD" "$@"
